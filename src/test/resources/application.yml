
spring:
  profiles:
    active: test

#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

  jpa:
    database: h2
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

#logging.level:
#  org.hibernate.type: trace

decorator:
  datasource:
    p6spy:
      enable-logging: false